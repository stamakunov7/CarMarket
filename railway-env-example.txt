# Railway Environment Variables
# Скопируйте эти переменные в Railway dashboard

# Database (Railway автоматически предоставит DATABASE_URL)
# Но можно также использовать отдельные переменные:
PGUSER=postgres
PGHOST=containers-us-west-xxx.railway.app
PGDATABASE=railway
PGPASSWORD=your_railway_password
PGPORT=5432

# JWT Configuration
JWT_SECRET=your_super_secret_jwt_key_change_this_in_production
JWT_EXPIRES_IN=7d

# Server Configuration
PORT=4000
NODE_ENV=production

# Redis (Railway автоматически предоставит REDIS_URL)
# Формат: redis://default:password@host:port

# Cloudinary Configuration (оставьте те же значения)
CLOUDINARY_CLOUD_NAME=your_cloudinary_cloud_name
CLOUDINARY_API_KEY=your_cloudinary_api_key
CLOUDINARY_API_SECRET=your_cloudinary_api_secret

# Telegram Bot Configuration
TELEGRAM_BOT_TOKEN=your_bot_token_here
TELEGRAM_CHAT_ID=your_chat_id_here

# Logging
LOG_LEVEL=info
